" .py file-specific vim rules

set cursorline   " Highlight the cursor's line
set expandtab    " Replace tabs with spaces
set textwidth=99 " Hard wrap
if exists('+colorcolumn')
	set colorcolumn=79
endif
